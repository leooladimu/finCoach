{
  "name": "fincoach-contradiction-detector",
  "version": "1.0.0",
  "description": "AWS Lambda function for detecting behavioral contradictions in financial data",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && zip -r function.zip index.js node_modules",
    "deploy": "npm run package && aws lambda update-function-code --function-name fincoach-contradiction-detector --zip-file fileb://function.zip",
    "test": "node test-local.js"
  },
  "keywords": ["fintech", "behavioral-analysis", "aws-lambda"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
